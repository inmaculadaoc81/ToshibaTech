const menuButton=document.querySelector('.menu-toggle'),navigation=document.querySelector('.nav');menuButton?.addEventListener('click',()=>{const open=navigation?.classList.toggle('open');menuButton.setAttribute('aria-expanded',String(Boolean(open)))});const form=document.getElementById('contactForm'),status=document.getElementById('formStatus');if(form){form.addEventListener('submit',async(event)=>{event.preventDefault();if(!form.reportValidity())return;const button=form.querySelector('button[type="submit"]'),original=button?.innerHTML||'Enviar consulta';if(button){button.disabled=true;button.textContent='Enviando…'}if(status)status.textContent='Enviando consulta…';const payload=Object.fromEntries(new FormData(form).entries());try{const response=await fetch('/api/contacto',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});let data={};try{data=await response.json()}catch{}if(!response.ok)throw new Error(data.code||'EMAIL_SEND_FAILED');form.reset();if(status)status.textContent='✓ Consulta enviada correctamente.'}catch(error){console.error('ToshibaTech formulario:',error);if(status)status.textContent='No se pudo enviar la consulta. Puedes llamarnos o escribirnos por WhatsApp.'}finally{if(button){button.disabled=false;button.innerHTML=original}}})}